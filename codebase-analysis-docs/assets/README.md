# assets/

This directory is intended for brain-dump outputs such as:
- Mermaid diagrams (saved as `.md` or `.svg` depending on workflow)
- ERD/schema diagrams
- JSON/YAML extracts of configuration

Current note: repository automated discovery (`search_files`) may be unavailable in this workspace, so only scaffold outputs may exist until tool support is restored.

