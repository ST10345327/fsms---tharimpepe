{
  "name": "tharimpepe-fsms",
  "version": "1.0.0",
  "description": "Feeding Scheme Management System",
  "scripts": {
    "mobile:sync": "npx cap sync",
    "mobile:copy": "npx cap copy",
    "mobile:run": "npx cap run android",
    "mobile:open": "npx cap open android",
    "server:start": "php -S 0.0.0.0:8080 -t \".\" \"router.php\""
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
