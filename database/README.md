# Database

This folder is reserved for database assets, migrations, and seed data as the project structure matures.

The current schema remains in `sql/schema.sql` because setup scripts still reference that path.
